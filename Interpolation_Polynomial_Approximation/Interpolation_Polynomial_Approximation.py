def Neville_Iterated_Interpolation(X, Y):
	"""
	To evaluate the onterpolating
	polynomial P on the n + 1 distinct
	numbers x0, ... ,xn at the number x for the function f

	X = x,x0, x1, ..., xn
	Y = f(x0), f(x1), ... , f(xn)
	x is the point where f(x) is not defined with the data given.
	The output of this if y where f(x) ~ y, or an estimation of the output.
	"""

	

